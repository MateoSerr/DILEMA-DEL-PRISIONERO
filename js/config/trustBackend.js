/**
 * Configuración del backend (Supabase). Si dejas URL y anon key vacíos, el juego funciona
 * solo en local sin cuentas (como antes). Para cuentas y datos en nube: crea un proyecto en
 * https://supabase.com , pega aquí la URL y la clave anon, y ejecuta el SQL en supabase/schema.sql
 */
window.TRUST_SUPABASE_URL = "";
window.TRUST_SUPABASE_ANON_KEY = "";
